function aliasGen(one, two){
    if (/^[a-zA-Z]+$/.test(one), /^[a-zA-Z]+$/.test(two)){
      var first = one[0];
      var last =  two[0];
      return (firstName.first, surname.last);
    }
    else {
      return 'Your name must start with a letter from A - Z.';
      }
}